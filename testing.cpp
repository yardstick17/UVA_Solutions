			/* AK_47*/
#include<iostream>
#include<stdio.h>
#include<vector>
#include<map>
#include<algorithm>
#include<map>
using namespace std;
#define S(x) scanf("%d",&x)
#define pb(x) push_back(x)
#define V(x) vector<x>
#define F(i,a,n) for(int i=(a);i<(n);++i)
#define REP(i,a,n) for(i=(a);i<(n);++i)

int main()
{
			string e,f;
			while(1)//scanf("%s%s",&e,&f)!= EOF)
			{	
		    	cin>>e>>f;
			//	scanf("%s %s",&e,&f);
				if(e.length()==0)
				break;
			}
			cout<<"Hah hahaha "<<endl;




return 0;
}

